<?xml version="1.0" encoding="utf-8"?>
<!-- Vignette du sélecteur pour le widget de groupe d'actions.
     Sans elle, Android retombait sur l'icône de l'application. L'exemple est
     figé : un aperçu n'exécute aucun code et ne connaît donc aucun groupe. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@drawable/widget_background"
    android:padding="10dp">

    <!-- Icône monochrome, comme la box depuis sa 2.23 — plus d'émoji. -->
    <ImageView
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:src="@drawable/preview_group_icon"
        android:scaleType="fitCenter"
        android:contentDescription="@null" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="2dp"
        android:gravity="center"
        android:text="Fermeture des volets"
        android:maxLines="2"
        android:textSize="14sp"
        android:textStyle="bold"
        android:textColor="#FF8080" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="4dp"
        android:gravity="center"
        android:text="6 actions"
        android:textSize="10sp"
        android:textColor="@color/widget_text_secondary" />
</LinearLayout>
